import { useEffect, useState } from 'react';
import { motion } from 'motion/react';
import { ArrowLeft, ArrowUpRight, Calendar, Clock, Menu, X } from 'lucide-react';
import { getStoryGridClass, storiesClasses } from './storiesLayout';
import { LiquidBackground } from './components/LiquidBackground';
import { DepthLayer } from './components/DepthLayer';
import { ScrollProgressBar } from './components/ScrollProgressBar';

type Story = { id:string; title:string; slug:string; excerpt:string; content:string; category:string; cover_url:string|null; featured:boolean; published_at:string|null; created_at:string };

const nav = [['Home','/'],['Stories','/stories'],['Services','/services'],['Portfolio','/portfolio'],['Process','/process'],['Inquire','/inquire']] as const;
const date = (value:string|null) => value ? new Intl.DateTimeFormat('en-NG',{day:'numeric',month:'long',year:'numeric'}).format(new Date(value)) : '';
const reading = (value:string) => Math.max(1,Math.ceil(value.trim().split(/\s+/).filter(Boolean).length/220));

function Header(){
  const [open,setOpen]=useState(false);
  return <header className="relative z-50 sticky top-0 border-b border-white/10 bg-black/40 px-4 py-3 backdrop-blur-md shadow-[0_4px_30px_rgba(0,0,0,.1)] md:px-[10%] md:py-5">
    <div className="flex items-center justify-between gap-5">
      <a href="/" className="text-lg font-bold uppercase tracking-wide bg-gradient-to-r from-white via-gray-200 to-gray-400 bg-clip-text text-transparent md:text-2xl">BLUEHAVEN STUDIOS</a>
      <nav className="hidden md:block"><ul className="flex gap-1 list-none">{nav.map(([label,href])=><li key={href}><a href={href} className={`relative block overflow-hidden rounded-lg px-3 py-2 text-sm uppercase tracking-wide transition hover:bg-white/10 ${label==='Stories'?'text-white':'text-white/80'}`}>{label}<span className="absolute bottom-0 left-0 h-0.5 w-0 bg-gradient-to-r from-blue-500 to-purple-500 transition-all duration-300 hover:w-full" /></a></li>)}</ul></nav>
      <button type="button" onClick={()=>setOpen(v=>!v)} className="rounded-lg p-2 text-white md:hidden" aria-label="Toggle menu">{open?<X size={24}/>:<Menu size={24}/>}</button>
    </div>
    {open&&<nav className="absolute left-0 right-0 top-full border-t border-white/10 bg-black/90 backdrop-blur-xl md:hidden"><div className="flex flex-col">{nav.map(([label,href])=><a key={href} href={href} onClick={()=>setOpen(false)} className="border-b border-white/10 px-6 py-4 text-sm uppercase tracking-wide text-white transition hover:bg-white/10">{label}</a>)}</div></nav>}
  </header>;
}

function Footer(){return <footer className="relative z-10 border-t border-white/10 bg-black/20 px-5 py-14 md:px-[10%] md:py-16"><div className="grid gap-10 md:grid-cols-[1.5fr_1fr_1fr]"><div><p className="text-xl font-black uppercase tracking-tight">Bluehaven Studios</p><p className="mt-3 max-w-sm text-sm leading-6 text-white/45">Creative direction, digital experiences and media built to make brands impossible to ignore.</p></div><div><p className="text-[10px] font-bold uppercase tracking-[.22em] text-white/35">Explore</p><div className="mt-4 flex flex-col gap-2 text-sm text-white/60">{nav.slice(0,5).map(([label,href])=><a key={href} href={href} className="transition hover:text-white">{label}</a>)}<a href="/stories" className="text-white">Stories</a></div></div><div><p className="text-[10px] font-bold uppercase tracking-[.22em] text-white/35">Connect</p><div className="mt-4 flex flex-col gap-2 text-sm text-white/60"><a href="/inquire" className="transition hover:text-white">Start a project</a><a href="/admin" className="transition hover:text-white">Admin</a></div></div></div><div className="mt-12 border-t border-white/10 pt-5 text-xs text-white/30">© {new Date().getFullYear()} BlueHaven Studios. All rights reserved.</div></footer>}

function StoryCard({story,index}:{story:Story;index:number}){
  const featured=index===0||story.featured;
  return <motion.a href={`/stories/${encodeURIComponent(story.slug)}`} className={`${getStoryGridClass(index)} group ${storiesClasses.card} text-left`} whileHover={{y:-4}} transition={{duration:.25,ease:'easeOut'}}>
    <div className={`${featured?'aspect-[16/8]':'aspect-[4/3]'} overflow-hidden bg-white/5`}>{story.cover_url?<img src={story.cover_url} alt="" loading="lazy" className="h-full w-full object-cover transition duration-700 group-hover:scale-105"/>:<div className="grid h-full place-items-center bg-gradient-to-br from-[#7f56d6]/20 via-white/[.02] to-[#ffde59]/10 text-xs font-bold uppercase tracking-[.25em] text-white/20">BlueHaven Studios</div>}</div>
    <div className={featured?'p-7 md:p-9':'p-6'}><div className="mb-3 flex items-center justify-between gap-3"><p className={storiesClasses.eyebrow}>{story.category||'Journal'}</p><ArrowUpRight size={17} className="text-white/30 transition group-hover:-translate-y-0.5 group-hover:translate-x-0.5 group-hover:text-[#ffde59]"/></div><h2 className={`${featured?'text-3xl md:text-5xl':'text-xl md:text-2xl'} font-bold leading-tight tracking-tight text-white`}>{story.title}</h2><p className="mt-3 text-sm leading-6 text-white/50">{story.excerpt}</p><div className="mt-6 flex flex-wrap gap-4 text-[10px] font-semibold uppercase tracking-[.16em] text-white/30"><span>{date(story.published_at||story.created_at)}</span><span>{reading(story.content)} min read</span></div></div>
  </motion.a>;
}

function Article({story}:{story:Story}){return <><main className={storiesClasses.section}><a href="/stories" className="inline-flex items-center gap-2 text-xs font-semibold uppercase tracking-[.18em] text-white/45 transition hover:text-white"><ArrowLeft size={15}/>All stories</a><div className="mt-10 border-b border-white/10 pb-12"><p className={storiesClasses.eyebrow}>{story.category||'Journal'}</p><h1 className="mt-4 max-w-5xl text-4xl font-black leading-[.95] tracking-[-.045em] md:text-7xl">{story.title}</h1><div className="mt-6 flex flex-wrap gap-5 text-xs uppercase tracking-[.12em] text-white/35"><span className="inline-flex items-center gap-2"><Calendar size={14}/>{date(story.published_at||story.created_at)}</span><span className="inline-flex items-center gap-2"><Clock size={14}/>{reading(story.content)} min read</span></div></div><article className="mt-10 grid gap-12 lg:grid-cols-[minmax(0,1fr)_260px]"><div>{story.cover_url&&<img src={story.cover_url} alt="" className="aspect-[16/8] w-full rounded-3xl object-cover"/>}<p className="mt-9 max-w-3xl text-lg leading-8 text-white/65 md:text-xl">{story.excerpt}</p><div className="mt-8 max-w-3xl whitespace-pre-wrap text-[16px] leading-8 text-white/75">{story.content}</div></div><aside><div className="sticky top-28 border-t border-white/10 pt-5"><p className="text-[10px] font-bold uppercase tracking-[.22em] text-white/35">BlueHaven Stories</p><p className="mt-3 text-sm leading-6 text-white/45">People, projects, lessons and ideas from behind the work.</p><a href="/inquire" className="mt-6 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.16em] text-[#ffde59]">Work with us <ArrowUpRight size={14}/></a></div></aside></article></main><Footer/></>}

export default function Stories(){
  const path=window.location.pathname; const slug=path.startsWith('/stories/')?decodeURIComponent(path.split('/').filter(Boolean)[1]||''):'';
  const [stories,setStories]=useState<Story[]>([]); const [story,setStory]=useState<Story|null>(null); const [loading,setLoading]=useState(true);
  useEffect(()=>{fetch(slug?`/api/stories?slug=${encodeURIComponent(slug)}`:'/api/stories').then(r=>r.json()).then(data=>slug?setStory(data.story||null):setStories(Array.isArray(data.stories)?data.stories:[])).catch(()=>slug?setStory(null):setStories([])).finally(()=>setLoading(false));},[slug]);
  return <div className={storiesClasses.page} style={{fontFamily:'Montserrat, sans-serif'}}><ScrollProgressBar/><DepthLayer depth={0}><LiquidBackground/></DepthLayer><Header/>{slug?(loading?<main className={storiesClasses.section}><p className="text-white/40">Loading story…</p></main>:story?<Article story={story}/>:<main className={storiesClasses.section}><h1 className="text-4xl font-black">Story not found.</h1><a href="/stories" className="mt-6 inline-flex items-center gap-2 text-[#ffde59]"><ArrowLeft size={16}/>All stories</a></main>):<><main className={storiesClasses.section}><div className="mb-12 max-w-3xl"><p className={storiesClasses.eyebrow}>Stories</p><h1 className="mt-3 text-5xl font-black tracking-tight md:text-7xl">Ideas behind the work.</h1><p className="mt-5 max-w-2xl text-base leading-7 text-white/50 md:text-lg">Behind-the-scenes notes, lessons, people and projects from BlueHaven Studios.</p></div><div className="mb-8 flex items-end justify-between gap-6 border-b border-white/10 pb-5"><div><p className="text-[10px] font-bold uppercase tracking-[.22em] text-white/35">Latest</p><h2 className="mt-2 text-3xl font-black tracking-tight md:text-4xl">Recent stories</h2></div><span className="hidden text-xs uppercase tracking-[.18em] text-white/30 md:block">{stories.length?`${stories.length} published`:'Editorial archive'}</span></div>{loading?<p className="py-10 text-white/40">Loading stories…</p>:!stories.length?<div className="border-y border-white/10 py-16 text-white/45">No stories have been published yet. Check back soon.</div>:<div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{stories.map((item,index)=><StoryCard key={item.id} story={item} index={index}/>)}</div>}</main><Footer/></>}</div>;
}
